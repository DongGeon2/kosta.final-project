<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosta.myproject.model.mapper.MemberMapper">
	<sql id="selectMember">
	select id,password,name,address 
	from spring_member
</sql>
<select id="findMemberById" resultType="memberVO">
	<include refid="selectMember"/>
	where id=#{value}
</select>
<select id="getMemberAddressKind" resultType="string">
	select distinct address from spring_member
</select>
<select id="findMemberByAddress" resultType="memberVO">
	<include refid="selectMember"/>
	where address=#{value}
</select>


<select id="getTotalCount" resultType="int">
	select count(*) from star_board
</select>
<select id="getPostingList" resultType="postVO" parameterType="int">
		select b.no,b.title,b.content,b.id 
 		from (select row_number() over(order by no desc) as rnum,
 		no,title,content,id from star_board) b, star_member m
 		where b.id=m.id and b.rnum between #{getStartRowNumber} and #{getEndRowNumber}
</select>
</mapper>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
















